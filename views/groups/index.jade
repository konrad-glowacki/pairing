extends ../layout

block content
  div.jumbotron
    h1
      span.glyphicon.glyphicon-fire(aria-hidden='true')
      &nbsp;
      = group.name
      
    h3 Witaj w grupie modlitwy wzajemnej!
    p.
      Wpisz swój adres e-mail na który będą przychodzić powiadomienia.
      Co tydzień będą losowane pary, które będą za siebie modlić. Zaczynamy od Wielkiego Postu.

    if errors
      div.alert.alert-danger(role='alert')
        strong Please repair errors below:
        ul
          each error in errors
            li= error.message

    form.form-horizontal(method='post', role='form', action='/groups')
      input(type='hidden' name='groupId' value=group.id)

      div.form-group
        div.col-sm-3
          input.form-control.input-lg(
            placeholder='Imię',
            required=true,
            name='name',
            type='text'
            value=name)

        div.col-sm-3
          input.form-control.input-lg(
            placeholder='Nazwisko',
            required=true,
            name='surname',
            type='text'
            value=surname)

        div.col-sm-4
          input.form-control.input-lg(
            placeholder='E-mail',
            required=true,
            name='email',
            type='email'
            value=email)

        div.col-sm-2
          button.btn.btn-primary.btn-lg(type='submit') Dodaj
